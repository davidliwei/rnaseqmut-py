#!/usr/bin/env python3
from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
PY_SUBDIR = ROOT / "python"
if str(PY_SUBDIR) not in sys.path:
    sys.path.insert(0, str(PY_SUBDIR))

from rnaseqmut_py.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
